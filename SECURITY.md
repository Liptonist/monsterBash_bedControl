# セキュリティポリシー

Monster Bash 救護所のベッド・椅子コントロール（`monsterBash_bedControl`）のセキュリティ方針と、脆弱性の報告方法です。

このアプリは**救護所で患者の氏名・年齢・性別・症状を扱います**。実際のイベント期間中は現場の運用がこのアプリに乗るため、報告・修正の扱いには少し特有の事情があります（後述の「イベント期間中の扱い」を読んでください）。

技術的な詳細は [README の「セキュリティについて」](README.md#セキュリティについて)にまとまっています。ここでは報告の窓口と、現時点で分かっている限界を書きます。

## 対象バージョン

| 対象 | サポート |
|---|---|
| `main` ブランチの最新（現行 v0.9.0） | ✅ |
| `/preview/` の公開分 | ✅（本番と同じ Firebase を見ています） |
| それ以前のコミット | ❌ |

過去バージョンへのバックポートはありません。修正は `main` に入れて再デプロイします。

## 脆弱性の報告方法

**公開の Issue や Pull Request には書かないでください。**このアプリは常時公開されていて、患者情報を扱います。

GitHub の非公開報告を使ってください。

1. リポジトリの **Security** タブを開く
2. **Report a vulnerability** から報告する

> リポジトリ管理者へ：この窓口は **Settings → Code security → Private vulnerability reporting** を有効にすると使えます。無効のままだと報告者に手段がありません。他の連絡先（メールなど）を使う場合は、ここに追記してください。

### 書いてほしいこと

- 何が起きるのか（誰が、何を、本来できないはずなのにできてしまうのか）
- 再現手順。可能なら、どのパス・どの操作かまで
- 影響範囲の見立て（患者情報が読めるのか、書き換えられるのか、消せるのか）
- 気づいたきっかけ

**実データでの検証はしないでください。**本番の Firebase は救護所が実際に使っているもので、テスト用の書き込みが現場の表示を壊します。読み取りだけで確認できる範囲、あるいはご自身の Firebase プロジェクトでの再現に留めてください。

### 対応の目安

個人が運用している小さなアプリなので、企業のような SLA はありません。

- 受け取りの返信：数日以内を目標
- 修正：内容と時期次第。深刻なものから
- 公開：修正後に、報告者の希望があればクレジットを記載

## イベント期間中の扱い

**イベント本番の直前・期間中は、セキュリティ修正であっても本番へ反映しないことがあります。**

理由は、このアプリが止まると救護所の運用が止まるためです。特に Realtime Database のセキュリティルールは、間違えると**全員が締め出され**、患者の受け入れ状況が誰にも見えなくなります。

そのため期間中は「その脆弱性を放置するリスク」と「変更でアプリが止まるリスク」を天秤にかけ、多くの場合はイベント終了後に修正します。緊急性が高い場合は、コードを変えずに済む対処（許可リストからの削除、Firebase コンソールでのルール変更、一時的なアクセス遮断）を優先します。

報告時に「いつまでなら待てるか」を書いていただけると助かります。

## この設計で守れること・守れないこと

### 守れること

`database.rules.json` が実質的な防御線です。画面側のログイン処理は案内のためのもので、防御にはなりません（誰でもデータベースに直接アクセスできるため）。

- 許可リストにいない人は、患者データを一切読み書きできない
- 一般スタッフは、確定した退室記録の書き換え・部屋の枠の一括削除・変更履歴の改ざんができない
- 変更履歴は追記のみで、管理者も含めて誰も消せない
- 書き込める内容の形（項目・型・文字数）が決まっていて、それ以外は弾かれる

詳細と検証方法は README を、実際の検証は `tools/rules-test.mjs` を見てください。

### 守れないこと（既知の限界）

報告いただく前に、以下は**把握済み**であることをご承知おきください。同じ内容の報告は不要です。

- **HTML に `apiKey` が埋まっていること**は仕様です。Firebase の `apiKey` は公開情報で、秘密鍵ではありません。防御はルールが担います
- **正規のスタッフの操作は制限できません。**ログインできる人は、その日の全患者の氏名・症状を読めます。誰が何をしたかは変更履歴に残りますが、閲覧そのものは記録されません。許可リストは必要な人だけに絞り、イベント後は棚卸ししてください
- **App Check は未導入です。**そのため、公開 `apiKey` を使った直接アクセスやログインの総当たりに対する、ルールより手前の層がありません（README に導入手順の概略があります）
- **ゴミ箱への移動・復帰は原子的ではありません。**「写す」「消す」の2回の書き込みなので、途中で失敗すると両方に残ることがあります。データが消えるのではなく重複する方向の失敗です
- **`/preview/` は本番と同じデータベースを見ています。**プレビュー専用のデータはありません

## 運用者向けの確認事項

イベントの前に、Firebase コンソールで次を確認してください。詳細は README の該当節にあります。

- [ ] **匿名認証が無効**になっている（有効だと誰でも `auth != null` を作れます）
- [ ] **「メールアドレスごとに1つのアカウント」が有効**になっている
- [ ] **許可リストが最新**である。前回のイベントの関係者や退任した人が残っていないか
- [ ] 認証アカウントを消すときは、**許可リストの側も必ず消す**。`authType: password` の登録だけが残ると、そのアドレスは第三者にパスワード認証情報を作られる余地が生まれます（ユーザー管理画面から消せばこの状態にはなりません）
- [ ] `database.rules.json` を変更したら、貼り替える前に `node tools/rules-test.mjs` を通す

## リポジトリに入れないもの

- パスワード、サービスアカウントの鍵、データベースの秘密鍵
- 患者情報を含むデータ（スクリーンショット、エクスポート、Issue への貼り付けを含む）

ログイン用のパスワードは Firebase Authentication が管理していて、Realtime Database にも、このリポジトリにも保存されません。
