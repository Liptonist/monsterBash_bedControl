# 当日のトラブル対応（FAQ）

救護所でアプリが思ったとおりに動かないときの対処です。**画面に出た文言でそのまま探せる**ように並べてあります。

---

## いちばん最初に

1. 画面の上に **「⚠️ オフライン中」** が出ていないか見る
   - **出ている → 再読み込みもタブを閉じるのもしないでください。**未送信の操作が消えます（[オフラインになったら](#オフラインになったら)）
2. 出ていなければ、**ページを再読み込み**する
3. それでも直らなければ、**別の端末で同じ操作**を試す
   - 直る → その端末だけの問題（ブラウザ・電波）
   - 直らない → 全体の問題。管理者へ

**迷ったら、患者の対応を優先して紙に書いてください。**あとから入力できます。

---

## ログインできない

| 画面に出る文言 | 原因 | その場の対処 |
|---|---|---|
| メールアドレスまたはパスワードが違います。（`auth/invalid-credential`） | 打ち間違い。または Google ログイン用として登録されていて、パスワードが未設定 | 打ち直す。それでも駄目なら管理者に「パスワードを設定してほしい」と依頼 |
| このブラウザでは Google ログインを完了できません…（`auth/missing-initial-state`） | LINE などの**アプリ内ブラウザ**、プライベートブラウズ、Cookie ブロック | **ID とパスワードでログイン**するのが最も速い。または「Safari で開く」で開き直す |
| 試行回数が多すぎます。（`auth/too-many-requests`） | 連続で失敗した | 数分待つ。急ぐなら別の人の端末を借りる |
| 通信に失敗しました。電波状況をご確認ください。（`auth/network-request-failed`） | 電波 | 場所を移る、テザリングに切り替える |
| このアカウントには利用権限がありません。 | 許可リストに未登録 | 管理者が `users.html` から追加。**メールアドレスの綴りを一字ずつ確認** |
| このURLからの Google ログインは許可されていません。（`auth/unauthorized-domain`） | 本番以外の URL を開いている | 正しい URL で開き直す。古い `/preview/` のブックマークは廃止済み |
| データベースの権限がありません（`PERMISSION_DENIED`） | 許可リストには居るが、ログイン方式の記録が合っていない | 管理者へ。`users.html` の「ログイン方法」列を確認してもらう |

> **会場で最も確実なのは ID とパスワードでのログインです。**この方式は Google のページを経由しないため、アプリ内ブラウザでも通ります。

---

## 保存されない・反映されない

| 症状 | 意味 | 対処 |
|---|---|---|
| 画面下に **「保存できませんでした: PERMISSION_DENIED」** | データベース側で書き込みが拒否された | **同じ操作を繰り返さないでください。**すぐ管理者へ。紙に記録して続行 |
| **「保存中です。反映をお待ちください...」** が消えない | まだサーバーに届いていない | **閉じない・再読み込みしない。**回線が戻れば送られます |
| 自分の画面では変わったのに、他の端末に出てこない | 相手の端末がオフライン、または古い画面のまま | 相手側で再読み込み。オフラインバナーが出ていないか確認 |
| 他の人の操作が自分の画面に出てこない | 自分の端末が切れている | 上と同じ。自分の画面を確認 |

### オフラインになったら

> ⚠️ オフライン中 — このページを閉じたり再読み込みしたりすると、未送信の操作は失われます。回線が戻るまでこのまま開いておいてください

- **やること** — そのまま開いて待つ。並行して紙に書く
- **やってはいけないこと** — 再読み込み、タブを閉じる、端末の電源を切る、ブラウザを終了する

回線が戻ると、開いたままの操作は自動で送信されます。

---

## 患者の登録・編集

| 画面に出る文言 | 意味 | 対処 |
|---|---|---|
| 漢字は使えません。カタカナで入力してください（例：ヤマダ タロウ）。 | 患者名に漢字が入っている | カタカナで入力し直す。ひらがなで打てば自動でカタカナになります |
| 本日同名の利用歴があります。同一患者ですか？ | 今日すでに同じ名前の人がいる | 同一人物なら、いったんキャンセルしてその記録を確認。別人（同姓同名）ならそのまま保存 |

**よくある戸惑い**

- **保存では空きに戻せません。**誤って登録した枠は「退室」してから、退室済み一覧で「ゴミ箱へ移動」してください
- **退室と移動は、漢字の名前のままでもできます。**過去に漢字で登録された患者を退室させられなくならないようにしてあります

---

## 枠（ベッド・椅子）の増減

| 画面に出る文言 | 意味 | 対処 |
|---|---|---|
| `B4` 使用中のため減らせません | いちばん後ろの枠に患者がいる | その患者を退室させるか、別の枠へ移動してから |
| ベッドを増やせませんでした（上限は40です）。 | 上限に達した | 増やせません |
| 枠を増やせませんでした。通信の状態を確かめて、もう一度お試しください。 | 通信 | 少し待って再試行 |

### 編集中に枠が消えたとき

別の端末が `−` でその枠を減らすと、「この枠は、ほかの端末で枠数が減らされたため、いまは無くなっています。」に続けて次のように聞かれます。**入力した内容は失われません。**

- 保存 → 「ベッドを1つ増やして、そこへ登録しますか？」
- 退室 → 「退室の記録だけ残しますか？」
- 移動 → 「移動先の枠へ登録し直しますか？」

いずれも「はい」で問題ありません。

---

## 移動

| 画面に出る文言 | 意味 | 対処 |
|---|---|---|
| 移動先の空きがありません | 空き枠がない | 先に移動先の部屋で枠を増やす |
| 選択した移動先はすでに使用中です。もう一度選び直してください。 | ほぼ同時に他の端末が同じ枠を使った | 別の枠を選び直す |

---

## 日次の締め（1日目の夜・管理者のみ）

**締めは取り消せません。**実行前に必ず文面を読んでください。

| 画面に出る文言 | 意味 | 対処 |
|---|---|---|
| ⚠️ 入室中の枠が N 件あります。 | まだ退室していない患者がいる | **先に退室処理を。**そのまま締めると、翌日に患者IDが重複する可能性があります |
| `2026-1` はすでに保存済みです。開催年・日を修正してからやり直してください。 | 同じ日を二重に締めようとした | 「開催年・日を修正」で日付を確認。すでに締め済みなら実行不要 |
| 締められませんでした: … | 通信または権限 | 少し待って再試行。繰り返すなら翌朝までに管理者が対応 |

---

## 表示・端末まわり

| 症状 | 対処 |
|---|---|
| 上に青いバーで「新しい版があります（v1.0.0）。」 | ［再読み込み］を押す。手が離せなければ ✕。放っておいても、操作していなければ1分ほどで自動的に読み込み直します（編集画面を開いている・入力中・保存中のときは自動では行いません） |
| 画面が勝手に暗くなる | タブ行の **☀️ スリープ防止** を押す（端末ごとに記憶。初期はオフ） |
| この端末では画面のスリープ防止を使えませんでした。 | 端末側の設定で画面が消えないようにする |
| **CSVで保存**や**日次の締め**のボタンが無い | 管理者だけの機能です。管理者に依頼してください |
| 文字が小さい・押しにくい | ブラウザの拡大。横画面にするとタブ行が1行になります |

---

## 当日やってはいけないこと

1. **Firebase コンソールでセキュリティルールを触る** — 間違えると全員が締め出され、患者の受け入れ状況が誰にも見えなくなります
2. **アプリを更新（デプロイ）する** — どうしても必要なときだけ、受付前か患者が途切れた時間に
3. **オフライン中に再読み込み・タブを閉じる** — 未送信の操作が消えます
4. **日次の締めを、1日目の夜以外に実行する** — 取り消せません

---

## それでも駄目なとき

**紙の運用に切り替えてください。**アプリの復旧より、患者の対応が優先です。

- 患者IDは手書きで連番を続ける（アプリの続きの番号から）
- 部屋・枠・氏名・症状・入室時刻を紙に控える
- 復旧後にまとめて入力する。入室時刻は編集画面で手入力できます

連絡先と、その日の管理者の名前は、この下に書き足しておいてください。

- 現場リーダー：
- アプリ管理者：
