name: Pages Environment Test (temporary)

on:
  workflow_dispatch:
  push:
    branches:
      - claude/feature-branch-html-publish-i9vvl7
    paths:
      - .github/workflows/pages-env-test.yml

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - name: Hello
        run: echo "hi, got a runner for the github-pages environment"
